/***************\
    SignalR
\***************/

/*
    This extension provides a mechanism for auto-refreshing a grid.
*/

( function ($, gp) {

    // listen for grid-ponent's init event
    $( document ).on( gp.events.init, function ( evt, config ) {

        if ( config.SignalR ) {



        }

    } );

} )( jQuery, gridponent );
